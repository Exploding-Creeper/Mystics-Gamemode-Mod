package com.mystic.gamemode.event;

/*import com.mystic.gamemode.client.ShowLocks;
import com.mystic.gamemode.usage.GameModeUsage;
import net.minecraft.entity.player.PlayerEntity;
import net.minecraft.inventory.Inventory;
import net.minecraft.screen.slot.SlotActionType;
import net.minecraft.util.ActionResult;

public class OnCreativeClick implements OnClickItemStackFromCreativeMenu{

    @Override
    public ActionResult interact(PlayerEntity playerEntity, Inventory inventory, int index, int x, int y, SlotActionType slotActionType) {
            if (GameModeUsage.getGameMode() == GameModeUsage.UNLOCKABLE) {
                if (slotActionType == SlotActionType.PICKUP) {
                    new ShowLocks(playerEntity, inventory, index, x, y);
                    return ActionResult.SUCCESS;
                }
            }
            return ActionResult.PASS;
    }
}*/

